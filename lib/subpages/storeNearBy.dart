import 'package:flutter/material.dart';
class StoreNearBy extends StatelessWidget {
  const StoreNearBy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
